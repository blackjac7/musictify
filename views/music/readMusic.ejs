<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Musictify</title>
</head>
<body>
    <%- include('../partials/navbar') %>
    <%- include('../partials/home') %>
    
    <h1>All Music</h1>
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Artist</th>
                <th>Genre</th>
                <th>Released Year</th>
                <th>see other user who liked this music</th>
                <th>Images</th>
                <th>refmusic</th>
                <th>add</th>
            </tr>
        </thead>
        <tbody>
            <% for (let i = 0; i < data.length; i++) { %> 
                <tr>
                    <td> <%= data[i].title %>  </td>
                    <td> <%= data[i].artist %>  </td>
                    <td> <%= data[i].genre %>  </td>
                    <td> <%= data[i].released_year %>  </td>

                    <td> <a href="/music/othersLiked/<%= data[i].id %>"> <button class="btn">see other user who liked this music</button></a></td>

                    <% if (data[i].imgData) { %>
                        <td><img src="/uploads/<%= data[i].imgData%>" alt="album Img" width="150" ></td>

                    <% } else { %>
                        <td>
                            <p>User does not upload cover music</p>
                        </td>
                    <% } %>
                    <td> 
                        <%= data[i].refMusics() %>
                    </td>
                    <td>
                        <a href="/music/addToMyPlaylist/<%= data[i].id %>"><button class="btn">add to playlist</button></a>
                    </td>
                </tr>
            <% } %> 
        </tbody>
    </table>

</body>
</html>